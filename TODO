Refactor / Restructure

db folder

# Things completed

- database done
- core / security done
- models done
- Schemas done
- crud now moving them to repositories (Working on), Look into where to put auth_crud ??

# Currently working on.

Split models into app/db/models and collect them in an **init**.py file.
Models created, relationships have been refined.

need to change routers to - api/v1 and add a deps.py file that holds
